source 'https://rubygems.org'

# Unpinned: the legacy Pages builder implied github-pages 84 (Jekyll 3.1.6,
# 2016), which no longer installs on a current Ruby. Tracking the current
# release keeps the same Jekyll 3.x semantics the site is written against,
# while actually being installable.
gem 'github-pages', group: :jekyll_plugins

# Bundled with Ruby < 3.4, standalone after.
gem 'csv'
gem 'base64'
gem 'bigdecimal'
